/* ============================================================
   教学内容库 —— 每一步的引导文案 / 检查项 / 期望动作
   ============================================================ */
export const STEPS = [
  {
    id:'intro', short:'认识设备',
    title:'① 认识 AD1200 音频解码器',
    html:`
      <p>AD1200 是 <em>1U 机架式音频解码器</em>，用在 <b>12 县</b>的信号源解码，解码后直接送发射机；
      尧山方向还承担 <b>微波 / 中央广播 / 桂林广播</b> 的备用信号。日常信源以 <b>IP 组播</b>为主。</p>
      <ul>
        <li>先把右侧实机转一圈：正面从左到右是 <em>厂商 LOGO、8 位数码管、Power/Module1-3 指示灯、▲▼◀▶ 方向键、Enter 与 Menu 键（并排：Enter 左、Menu 右）</em>；◀ ▶ 只是保留键位，本机未定义功能</li>
        <li>点 <b>背视图</b>：本机装在 <em>19 英寸机柜</em>里（左右立柱 + 安装耳螺钉）。<em>上方 2U 是上一级交换机</em>，<em>下方 2U 就是 200W 调频发射机</em>；
            三台设备之间留了机位间隙，<b>任何一台都不会挡住另一台</b></li>
        <li>下方发射机的前面板上能看到 <b>频率（在 88.3 附近轻微漂动）</b>、<b>输出功率（约 170~200W）</b> 与电平柱，工作时会跟着动</li>
        <li>本机背板上左边是 ASI 输入/输出 BNC，中间三块解码板卡 = 三路音频，每路左右声道各一个圆柱形三芯接口（蓝环 L / 红环 R），右侧是本机<em>唯一的 RJ45 网口</em>，最右是 AC 220V 电源、开关与接地柱</li>
        <li>背板线缆已按现场接好：<em>AC 220V 电源线、ASI 输入同轴线、3 路卡侬（XLR）输出线</em>，其中 <b>2 根卡侬线</b>把解码模块1 的左右声道直送下方发射机的 <b>250W 功放模块</b>（接在发射机<em>后面板</em>）；鼠标悬停任意一根线可看它的作用</li>
        <li><b>网线走向要看清</b>：一端插在 <em>上级交换机的“后面板” VLAN30 业务口</em>，另一端插本机<em>背板的 NET 口</em>，
            整根线走机柜后侧 —— 与现场一致，也不会绕到前面来挡面板</li>
        <li>把视角拉到交换机正面：它的铭牌上写着 <b>VLAN 已设置完成</b>（管理 VLAN10 = 192.168.1.0/24，业务 VLAN30 = 组播 236.171.30.5）。<em>交换机不用动，接好线就行</em></li>
        <li>鼠标悬停到任意部件上会显示名称，<b>长按 Menu 键</b>可进入设置菜单</li>
        <li>左侧模拟浏览器窗口可<b>最大化 / 拖动 / 缩放</b>：双击标题栏或点 <em>⤢</em> 最大化，拖右下角缩放，点 <em>⇲</em> 还原</li>
      </ul>
      <p>点击 <b>下一步</b> 开始配置。</p>`,
    checks:[{k:'seen-device', t:'已浏览实机正面与背面'}]
  },
  {
    id:'sameNet', short:'同网段要求',
    title:'② 为什么电脑必须与设备同网段',
    html:`
      <p>设备出厂管理 IP 是 <code>192.168.1.150</code>，掩码 <code>255.255.255.0</code>。
      电脑要能打开它的后台，<em>二者必须在同一网段</em>。</p>
      <ul>
        <li>同网段内主机直接 ARP 广播寻址，不经过网关即可互通</li>
        <li><code>192.168.1.x / 255.255.255.0</code> → 与设备同网段 ✅</li>
        <li><code>192.168.2.x / 255.255.255.0</code> → 不同网段 ❌，浏览器会超时</li>
        <li>把mask放宽成 <code>255.255.0.0</code>，设备侧网段就能覆盖电脑，也能通（现场应急做法）</li>
      </ul>
      <p><b>上级交换机的 VLAN 已经设置完成</b>（VLAN10 管理段 / VLAN30 业务段，端口已划分并保存），
      运维电脑的网线插在交换机的 VLAN10 接口，所以<em>电脑侧只要 IP 与设备同网段就能通</em>，不需要再配交换机。</p>
      <p>下一步先<em>在实机上查 IP</em>，再按查到的结果配电脑。</p>`,
    checks:[{k:'understood-subnet', t:'已理解同网段原理'}],
    onEnter:()=>{}
  },
  {
    id:'checkIp', short:'实机查 IP',
    title:'③ 长按 Menu 键，在数码管上读出设备 IP',
    html:`
      <p>设备没有屏幕，IP 要从<em>前面板数码管</em>上读。<b>注意时序：长按 Menu 只显示菜单项名，必须按 Enter 进页才会显示数值。</b></p>
      <pre class="seq">长按 Menu  → [IP ADD]      只显示菜单项名，不显示数值
   Enter   → [A1-192]      进页后才显示数值（第 1 段）
   ▲       → [A1-168]      逐段翻页（每屏 3 位）
   ▲       → [A1-001]
   ▲       → [A1-150]
   Enter   → [192.168.]    切换为完整显示
长按 Menu  → 退出菜单</pre>
      <ul>
        <li>先 <b>长按 Menu 键</b>（约 0.65 秒）进入设置菜单，数码管只显示菜单项名 <code>IP ADD</code></li>
        <li>菜单项都是<b>大写</b>显示：<code>IP ADD → IP MASK → GATEWAY → MCAST IP → MCAST PORT → MAC ADD → SOFT VER → EXIT</code>，
            按 <b>▼</b> 或<b>短按 Menu</b> 逐项切换（切换时同样只显示菜单项名）</li>
        <li>停在 <code>IP ADD</code> 这一页，按 <b>Enter 键进入</b>，数码管从第 1 段开始显示 <code>A1-192</code></li>
        <li>再按 <b>▲ 上键</b> 逐段翻看 IP 的四段：
            <em>A1-192</em> → <em>A1-168</em> → <em>A1-001</em> → <em>A1-150</em>（每屏 3 位，循环）</li>
        <li><code>MCAST IP</code> / <code>MCAST PORT</code> 两页可以在机房里直接回读设备当前生效的组播地址与端口，
            不用开电脑也能确认组播收的是哪一路（同样要先按 Enter 进页）</li>
        <li>再按一次 <b>Enter 键</b> 可切到“完整显示”，再按一次回到逐段查看</li>
        <li>看完再 <b>长按 Menu 键</b> 退出菜单</li>
      </ul>
      <p>请读出本机的 IP = <em>192.168.1.150</em>，掩码 <em>255.255.255.0</em>（在 <code>IP MASK</code> 页同理：Enter 进页 → ▲ 翻段）。</p>`,
    checks:[
      {k:'menu-opened', t:'已长按 Menu 键进入菜单（数码管只显示 IP ADD）'},
      {k:'ip-read',     t:'已按 Enter 进页并用 ▲ 逐段读出 A1-192 → A1-168 → A1-001 → A1-150'}
    ]
  },
  {
    id:'configPc', short:'配置电脑 IP',
    title:'④ 把本机电脑配到同网段',
    html:`
      <p>在左侧"本机网络配置"里填入：</p>
      <ul>
        <li>IP 地址：<code>192.168.1.50</code>（与设备同一网段，且不能与设备相同）</li>
        <li>子网掩码：<code>255.255.255.0</code></li>
        <li>网关：<code>192.168.1.1</code>（同网段直连时可留空）</li>
      </ul>
      <p>点击 <b>应用配置</b>，再点 <b>测试连通性</b> 验证能否 ping 通设备。</p>
      <p>测试过程与实机一致：<em>应用配置 → 同网段（徽标转黄）→ 测试连通性 → ping 通（徽标与按钮转绿，显示“已连通 · 连接绿色”）</em>，
      同时右侧孪生里那根<em>网线会整根亮绿</em>、网口指示灯转绿 —— 一眼就能看出链路真的通了。</p>`,
    checks:[
      {k:'pc-ip',        t:'本机 IP 已设为 192.168.1.x'},
      {k:'pc-reachable', t:'连通性测试通过'}
    ]
  },
  {
    id:'browse', short:'登录后台',
    title:'⑤ 浏览器访问设备后台并登录',
    html:`
      <p>在模拟浏览器地址栏输入设备 IP，回车：<code>http://192.168.1.150</code></p>
      <ul>
        <li>出现 <b>Welcome to Circloop</b> 登录页（与原厂 1:1 一致）</li>
        <li>登录页的<b>用户名 / 密码输入框每次打开都是空的</b>，<em>不预填、不记忆</em>，必须学员自己敲进去</li>
        <li>页面左下角会<b>像打字机一样把账号密码明文打出来</b>给学员看：用户名 <em>dtv</em>，密码 <em>123</em>
            （教学场景故意显示明文，实机上切勿外泄）</li>
        <li>输错会自动清空输入框，需要重新输入；正确后才会跳后台</li>
        <li>注意地址栏里的 IP 要和你刚配好的电脑 IP <b>对应得上</b></li>
      </ul>`,
    checks:[
      {k:'navigated', t:'已访问 http://192.168.1.150'},
      {k:'logged-in', t:'已用 dtv / 123 登录成功'}
    ]
  },
  {
    id:'multicast', short:'设组播地址',
    title:'⑥ 设置组播地址与端口（最关键）',
    html:`
      <p>登录后进入 <b>设备参数 → 组播设置</b>，这一项决定设备收哪一路节目：</p>
      <ul>
        <li>组播地址：<code>236.171.30.5</code>（组播地址范围 <b>224.0.0.0 ~ 239.255.255.255</b>）</li>
        <li>端口：<code>4000</code>（范围 1~65535）</li>
        <li>组播源过滤使能保持 <b>ON</b>；<b>组播源地址不用设置</b> —— 它会自动跟随组播地址显示（只读）</li>
        <li>「输入输出设置」里的 <b>自动切换保持默认“自动”</b>，输入源为 <b>IP</b>，都不用改</li>
      </ul>
      <p>点 <b>确认</b> 保存。填错会被拒绝并给出原因，可以放心试错。</p>
      <p><b>注意：组播地址没设置生效之前，解码模块里点“搜索”是搜不出任何节目的</b>
      （设备根本没加入组播组），会明确提示你先来这一页把组播参数设好。</p>`,
    checks:[{k:'mcast-ok', t:'组播参数 236.171.30.5:4000 已保存生效'}]
  },
  {
    id:'search', short:'搜索节目',
    title:'⑦ 搜索节目源',
    html:`
      <p>进入 <b>解码模块1设置</b>，点工具栏的 <b>搜索</b>：</p>
      <ul>
        <li>设备会解析 PAT → PMT → SDT，列出全部节目并自动填写 PCR / Audio PID</li>
        <li>正常情况下能搜到 <em>6 个广播节目</em>：中国之声、音乐之声、经济之声、桂林新闻综合、桂林旅游、发射台测试频率</li>
        <li>红色指示灯代表<em>未锁定</em>，未选节目前一直是红的</li>
      </ul>
      <p>点 <b>搜索</b> 试试。搜索过程中不要重复点击。</p>
      <p>三块板卡（解码模块1/2/3）都可以各自搜索：点顶部 <b>板卡1/2/3</b> 或左侧菜单切换，
      切回某块板卡时它上次搜到的节目列表和已保存的频道都还在。</p>
      <p>如果组播地址还没设置，搜索会直接中止并给出原因 —— 先回 <b>设备参数 → 组播设置</b> 把地址端口设好再来。</p>`,
    checks:[
      {k:'searched', t:'已完成节目搜索'},
      {k:'found-6',  t:'搜到 6 个节目（含 6 路广播）'}
    ]
  },
  {
    id:'play', short:'选节目出声',
    title:'⑧ 选择节目 → 锁定 → 试听',
    html:`
      <p>在列表里点任意一路节目（<b>点节目名称那一行</b>），例如 <em>中国之声</em>：</p>
      <ul>
        <li><b>点一下频道就自动保存并锁定这个频道</b>（不用再点别的按钮，也不要点“确认”）；要换台就直接点另一行，覆盖保存</li>
        <li><b>板卡 1 / 2 / 3 都可以点</b>：左侧「解码模块1/2/3设置」，或页面顶部「板卡1 / 板卡2 / 板卡3」按钮直接切；
            每块板卡各自记自己的搜索结果与已保存频道，切来切去都不会丢</li>
        <li>已保存的板卡按钮上会出现 <b>✓</b>，顶部还会显示「本板卡已保存频道：[101] 中国之声」</li>
        <li>网页会开始 <b>播放该节目的声音</b>（模拟器内置音调，可调音量）</li>
        <li>实机侧的 <b>LOCK 指示灯转绿</b>，数码管显示 <code>LOCK</code></li>
        <li>网页里那一行显示<b>播放中（绿色）</b>，工具栏显示"当前节目: 101 中国之声（已锁定 · 已保存）"</li>
        <li>音频经 <b>2 根卡侬线</b>（本机背板输出 → 发射机后面板 250W 功放模块）送下去，调制后由 RF OUT 上天馈</li>
        <li>拖动 <b>音量</b> 滑块可实时改变输出音量</li>
      </ul>
      <p>六路节目都可以逐个点击试听，确认哪一路要送发射机。</p>`,
    checks:[
      {k:'picked', t:'已选择一路节目'},
      {k:'locked', t:'节目已锁定（网页绿灯 + 实机 LOCK 指示）'}
    ]
  },
  {
    id:'api', short:'接口调试',
    title:'⑨ 接口与调试说明',
    html:`
      <p>设备对外接口分三类，排障时按这个顺序查：</p>
      <ul>
        <li><b>网口（RJ45，背板唯一一个）</b>：HTTP 80 端口提供 WEB 配置；SNMP 162/UDP 上报 Trap；
            也支持 <code>telnet</code> 方式执行 <code>show ip</code> / <code>show lock</code></li>
        <li><b>组播接收（与网口共用）</b>：IGMPv2/v3 加入组播组，建议交换机开启 <em>IGMP Snooping</em>，
            否则多路组播会把上行打成广播风暴</li>
        <li><b>ASI IN / OUT（BNC）</b>：270Mb/s，用作 IP 信源的第二备份；
            <b>音频输出端子</b>：凤凰端子接发射机音频输入，注意左右声道（AL/AE1…）对应关系</li>
      </ul>
      <p>常用排查思路：</p>
      <ul>
        <li>打不开后台 → 查同网段 / IP 冲突 / 网口指示灯</li>
        <li>搜不到节目 → 查组播地址端口是否与前端一致、是否被过滤、IGMP 是否正常</li>
        <li>有节目但不锁定 → 查 PID、查前端码流、查 ASI 备份链路是否启用自动切换</li>
      </ul>
      <ul>
        <li><b>机柜链路</b>：上级交换机（2U，VLAN 已设置完成，<em>后面板</em> VLAN30 业务口）→ 网线沿机柜后侧下行 → 本机背板 NET 口；
            AD1200 背板输出 → 2 根卡侬线 → 下方 200W 发射机 250W 功放（XLR 音频输入）→ RF OUT 上天馈。
            排障时先看这 4 段链路谁没接好</li>
        <li><b>输出通道</b>：背板三块板卡对应三路音频输出，<em>每一路都分左右声道</em>，共 6 个圆柱形三芯（3 口）接口
            （模块1 → L/R，模块2 → L/R，模块3 → L/R；蓝环 = 左声道 L，红环 = 右声道 R）。在右侧孪生背视图上直接点接口，即可把试听输出切到该通道。</li>
      </ul>
      <p><b>AI 提问（演示）</b>：后台右下角有个 <b>AI 提问</b> 悬浮球（顶栏也有同名按钮），点开可以问
      「数码管怎么读 IP」「组播地址怎么设」「搜不到节目怎么办」「板卡怎么切」「选中怎么保存」等。
      这是<em>教学演示</em>：回复是预先写好的演示内容，<b>没有接入真实大模型</b>，用来演练“一边操作一边问”的节奏。</p>
      <p>后台页面已按原厂界面 <b>1:1 还原</b>（含 8 位数码管用像素字体显示，IP / 菜单名都能完整显示），
      底部不再有额外消息栏，操作记录统一汇总到本页右下角的日志面板里。</p>
      <p>教学到此结束。可点右上 <b>重置实验</b> 重新练一遍；演示时建议点 <b>⛶ 全屏演示</b>，画面会铺满整个屏幕。</p>`,
    checks:[{k:'done', t:'全部步骤完成'}]
  },
];
export const STEP_INDEX = Object.fromEntries(STEPS.map((s,i)=>[s.id,i]));
